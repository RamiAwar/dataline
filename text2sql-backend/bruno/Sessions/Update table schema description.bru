meta {
  name: Update table schema description
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/schemas/table/:table_id
  body: json
  auth: none
}

body:json {
  {
      "description": "Actor name"
  }
}
